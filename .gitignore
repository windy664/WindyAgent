# MacOS DS_Store files
.DS_Store

# Gradle cache folder
.gradle

# Gradle build folder（所有模块的 build/ 均忽略）
**/build/

# IntelliJ
out/
.idea
*.iml
# mpeltonen/sbt-idea plugin
.idea_modules/

# virtual machine crash logs, see http://www.java.com/en/download/help/error_hotspot.xml
hs_err_pid*

# Common working directory
run

# 已搁置的 NeoForge 载体：本地留作备份，不进版本库
disabled-neoforge/
build.gradle.neoforge-backup

# Claude Code 本地配置（含权限规则，非项目源码）
.claude/

# 落地部署样例（模板配置，非源码；技能副本已在 core/resources/skills/ 管理）
deploy/

# 第三方依赖 jar（CMI 等，构建走 jitpack/compileOnly，不入库）
libs/

# 内置只读参考库的爬取内容（官方 wiki 转 md：CMI / DecentHolograms 等）——
#   规避 DMCA 不入库；本地磁盘保留，构建时照常打进 jar。要重建就重跑爬虫脚本。
#   代码（ReferenceLibrary.kt）在缺资源时优雅降级为空库，clone 出来能编能跑。
core/src/main/resources/knowledge-ref/

# 知识库爬虫脚本（把官方 wiki 爬成上面的 pack）——durable 本地留存、不入库（同 DMCA 顾虑）。
#   见 tools/knowledge-crawler/README.md。要重建 pack 就在这里 npm install && npm run <pack>。
tools/knowledge-crawler/

# 前端构建产物与依赖（web-ui 源码入库，node_modules/dist 不入库）
web/web-ui/node_modules/
web/web-ui/dist/

# 不公开的开发日志
DEVLOG.md
DEVELOPMENT.md
